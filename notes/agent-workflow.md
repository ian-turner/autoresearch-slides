# Agent Workflow

- The repository now includes a root `AGENTS.md` file with change-discipline instructions.
- Future work should record durable implementation context in `notes/` rather than embedding process notes in slide source files.
- Changes to `main.tex` and other LaTeX files should stay tightly scoped to the requested content unless a small readability adjustment is needed to keep the deck coherent.
