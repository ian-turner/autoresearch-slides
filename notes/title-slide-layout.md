# Title Slide Layout

- The title slide title box is set to `0.9\paperwidth` in `main.tex`.
- That width keeps the current subtitle, `Autonomous Research and Software Development with LLM Agents`, on a single line.
- If the title page copy changes again, adjust the title-page `beamercolorbox` width before reducing font sizes.
